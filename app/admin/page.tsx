import { AdminLayout } from "@/components/admin/admin-layout"

// /admin is the local settings surface (AI provider, model, voice).
export default function AdminPage() {
  return (
    <AdminLayout />
  )
}
