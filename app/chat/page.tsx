// Classic layout: sidebar + main panel without the command-center nav rail.
import { ChatInterface } from "@/components/chat/chat-interface"

export default function ChatPage() {
  return (
    <ChatInterface layoutMode="classic" />
  )
}
